import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const move = () => {
  return (
    <View>
      <Text>move</Text>
    </View>
  )
}

export default move

const styles = StyleSheet.create({})